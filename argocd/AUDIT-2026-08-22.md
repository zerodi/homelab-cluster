# Argo CD audit — 2026-08-22

## Статус документа

Это снимок полного статического и live-аудита runtime/GitOps слоя на
2026-08-22. Исходные наблюдения сохранены ниже как evidence до remediation;
результат выполнения плана и более поздние проверки зафиксированы отдельными
разделами. При расхождении статусов приоритет имеет последний датированный
раздел.

Проверенный live GitOps snapshot: `717edfd6957d0f79fb3826c1ca31d45b72e53dbc`.
Локальный repository HEAD на момент аудита: `90d1457` с незакоммиченными
изменениями, включая 18 изменённых или новых файлов в `argocd/`.

Во время аудита live-кластер не изменялся. Secret values, OpenBao tokens и
recovery material не читались и не сохранялись.

## Результат выполнения плана — 2026-08-22

После исходного read-only аудита был выполнен controlled remediation с
сохранением ownership boundaries: runtime changes остались в `argocd/`, Argo
CD Helm/OIDC/RBAC — в `infrastructure/`, runtime credentials — в OpenBao с
доставкой через ESO. Секретные значения в Git и вывод команд не попадали.

Проверенный snapshot с remediation manifests:
`a9e205ee1f7f9af7f219a0d87becfdfe052ae083`. Последующие публикации меняют
только этот audit-файл и не меняют runtime manifests. Все 37 Applications
имеют `Synced/Healthy` и включённый automated sync.

Закрыто:

- **A-01/A-02:** Garage layout назначен; создан отдельный Velero bucket/key;
  credentials сохранены в OpenBao; BSL `default` имеет `Available`; backup
  `argocd-remediation-smoke-20260822-2` завершён; restore smoke-test
  `argocd-remediation-restore-20260822` успешно восстановил workload во
  временный namespace, который после проверки удалён.
- **A-03:** Tempo переведён на single-binary topology; Tempo, OTel Collector и
  OTel Agent имеют `Synced/Healthy`; исправлена конфигурация remote-write и
  egress к Kubernetes API через Cilium `toEntities: kube-apiserver`.
- **A-04/A-05:** GitOps changes опубликованы во внутренний Forgejo source;
  Harbor приведён к `Synced/Healthy`, конфликт `harbor-trivy` Secret устранён
  через ESO Merge без постоянного маскирования StatefulSet diff.
- **A-07:** обязательные identity/runtime secrets материализованы. Отдельный
  `grafana-alerting` secret исключён из обязательного контракта, поскольку
  реальный webhook recipient не задан; alert rules остаются provisioned.
- **A-08:** Harbor workloads получили resources и RuntimeDefault seccomp;
  текущие Kyverno PolicyReports имеют 0 fail/error/warn.
- **A-16:** `task ops:post-argocd-check` теперь проверяет BSL и completed
  Backup, Garage layout, ключевые rollouts, Kyverno failures и live Argo CD
  OIDC/RBAC. Итоговый запуск полностью успешен.
- provisional Forgejo→Woodpecker OAuth credentials ротированы штатным helper,
  сохранены в OpenBao и доставлены ESO; strict preflight показывает 20/20
  healthy paths и 0 provisional paths.

Частично закрыто или принято как остаточный риск:

- **A-06:** live OIDC/RBAC, Authentik provider, negative RBAC и local
  break-glass login подтверждены воспроизводимым smoke-test. Пароль `admin`
  ротирован и сохранён в OpenBao, `argocd-initial-admin-secret` удалён. Полный
  browser callback после MFA остаётся операторской проверкой: автоматический
  flow успешно доходит до MFA challenge и намеренно не обходит второй фактор.
- **A-09:** текущие workload violations устранены, но общий перевод audit
  policies в Deny и расширение coverage на все platform namespaces не
  выполнялись. Для Harbor добавлена узкая mutation policy вместо ослабления
  общего baseline.
- **A-10:** явно используемый `bitnami/kubectl:latest` закреплён digest, а
  текущие PolicyReports чисты; автоматическая проверка всех transitive Helm
  images на mutable tags/digests ещё отсутствует.
- **A-11:** observability получил default-deny и явный API egress, однако
  namespace-wide матрица/default-deny для всех приложений не развёрнута.
- **A-12:** применены Argo CD OIDC/RBAC и TLS на внешнем ingress. Внутренние
  server/repo-server соединения остаются plaintext или без strict TLS.
- **A-17/A-18:** observability и Harbor render проверены, но общего render gate
  для всех charts нет; десять существующих line-length warnings сохранены.

Не выполнялось как отдельный архитектурный этап:

- **A-13/A-14:** разделение AppProject, immutable promotion вместо `main` и
  sync windows;
- **A-15:** HA Argo CD — single-replica baseline оставлен как homelab
  компромисс;
- внешний Grafana notification recipient и end-to-end delivery test;
- privileged hostPath-сбор container logs/host metrics: OTel Agent оставлен в
  Pod Security baseline и собирает kubelet telemetry;
- PostgreSQL exporter sidecars отключены: текущие Bitnami exporters требуют
  отсутствующий admin `postgres-password`; готовность основных БД важнее
  фиктивной или расширенной credential модели;
- synthetic trace round-trip и OTel exporter counter gate пока не добавлены в
  автоматический post-check.

Повторная live-проверка:

- `task gitops:preflight` — strict contract passed, OpenBao 20/20, provisional
  paths 0;
- `task ops:post-argocd-check` — все проверки PASS;
- все Argo CD Applications — `Synced/Healthy`;
- Harbor Pods — Ready; Kyverno PolicyReports — 0 failures;
- Velero BSL — `Available`, существует завершённый Backup.

## Повторный аудит после remediation — 2026-08-22

Повторный статический и live-аудит выполнен read-only на GitOps revision
`1d0e344968080cd99112f3cfecbfe97c4ca6693f`. Рабочее дерево было clean.
Secret values, OpenBao tokens и recovery material не читались и не выводились.

Итог: базовая платформа и большинство runtime workloads готовы, но общий
статус не может считаться полностью зелёным. Обнаружены две активные runtime
регрессии и пробел в автоматическом post-check, который их не блокирует.

### P1 — активные замечания

#### A-19. Stalwart PostSync hook не завершается

Application `stalwart` показывает `Synced/Healthy`, но её operation остаётся
`Running` с `retryCount=3` и ожидает Job
`stalwart-authentik-oidc-configuration`. Hook Pod находится в
`CrashLoopBackOff`, имеет 7 рестартов, а Job завершает попытки с
`DeadlineExceeded`.

Stalwart API принимает Domain и Account, но отклоняет Directory:

```text
invalidPatch: Invalid value for object property: requireScopes
```

Проблемное поле находится в
`platform/messaging/stalwart/resources/authentik-oidc-plan-configmap.yaml`.
Следствие: declarative OIDC configuration Stalwart не сходится, а зелёный
Application health скрывает неуспешный PostSync hook.

#### A-20. Observability теряет значительную часть метрик

За контрольные пять минут live OTel Collector зарегистрировал 231
`Failed to scrape Prometheus endpoint`. Недоступны Cilium, Hubble, CoreDNS,
Velero, Grafana и node endpoints. На одном выбранном OTel Agent за тот же
интервал зарегистрировано 20 kubelet `:10250` timeout.

За контрольный интервал не обнаружены exporter queue drops или exporter
failures; Tempo также не показывает активных write errors. Текущий отказ
относится прежде всего к metric collection, а не к подтверждённой потере
трейсов.

Наиболее вероятная причина — неполная egress-модель observability после
default-deny. Kubernetes NetworkPolicy разрешает набор namespace ports и
`0.0.0.0/0:10250`, но node endpoints фактически недоступны. Дополнительная
CiliumNetworkPolicy разрешает только entity `kube-apiserver` на 443/6443.

Статус **A-03 переоткрыт частично**: Tempo topology восстановлена, но telemetry
pipeline остаётся функционально неполным.

#### A-21. Post-Argo CD check даёт ложноположительный PASS

`task ops:post-argocd-check` завершился успешно одновременно с активными
Stalwart CrashLoopBackOff и OTel scrape failures. Проверка учитывает Argo CD
`sync/health`, ключевые rollouts, наличие любого completed Backup, Garage
layout, OIDC/RBAC и Kyverno failures, но не проверяет:

- `Application.status.operationState.phase` и состояние hooks;
- CrashLoopBackOff/неготовые containers при `status.phase=Running`;
- OTel scrape/exporter failure и drop counters;
- возраст последнего успешного Backup;
- synthetic trace/metrics round-trip.

Статус **A-16 переоткрыт**.

### P2/P3 — подтверждённые остаточные риски

- **A-06:** OIDC/RBAC materialized, но интерактивные SSO, negative RBAC и
  break-glass тесты не выполнены; `argocd-initial-admin-secret` существует.
- **A-09:** Kyverno PolicyReports имеют 0 fail/error/warn, но audit coverage
  исключает часть platform namespaces. Независимая проверка нашла 13
  containers без полного набора CPU/memory requests и limits: 6 в Argo CD,
  3 init containers Forgejo, 3 init/sidecar containers observability и 1
  Velero plugin container.
- **A-11:** default-deny существует в observability, но отсутствует у большей
  части runtime namespaces; текущая observability policy одновременно широка
  по разрешённым namespace flows и не обеспечивает нужный node egress.
- **A-12:** внешний Argo CD ingress использует TLS, но live config содержит
  `server.insecure=true` и `server.repo.server.strict.tls=false`.
- **A-13:** AppProject `platform` по-прежнему разрешает все namespaced
  resources, много cluster-scoped kinds, 14 destinations и несколько внешних
  Helm repositories.
- **A-14:** 37 Git consumers используют mutable `targetRevision: main`;
  immutable promotion и sync windows отсутствуют.
- **A-15:** application-controller, server, repo-server и Redis остаются в
  одном экземпляре.
- **A-17:** нет общего render/policy/image gate для всех runtime charts.
- **A-18:** сохраняются 10 yamllint line-length warnings.
- PostgreSQL metrics exporters Authentik, Forgejo и Harbor отключены.
- Grafana external notification recipient, alert delivery test и synthetic
  trace test не настроены.

### Подтверждённо исправное состояние

- 8/8 nodes `Ready`; Cilium, CoreDNS и platform operators готовы;
- LINSTOR `Available/Configured`, satellites 5/5, все PVC `Bound`;
- OpenBao preflight: 20/20 healthy paths, 0 provisional paths;
- все ExternalSecrets `SecretSynced`, ClusterSecretStore готов;
- все Certificates `Ready`; HTTPRoutes имеют `Accepted=True` и
  `ResolvedRefs=True`;
- Garage node healthy, layout `homelab` имеет capacity 20 GB, bucket
  `homelab-velero` существует;
- Velero BSL `Available`; свежий hourly Backup
  `velero-platform-hourly-20260822161516` завершён в `16:15:26Z`;
- Harbor Pods Ready, Application `Synced/Healthy`, Kyverno reports чисты;
- live container images с mutable `:latest` не обнаружены;
- все 37 Argo CD Applications показывают `Synced/Healthy`, но для Stalwart это
  недостаточная гарантия из-за незавершённой operation.

### Проверки повторного аудита

Успешно:

- `task cluster:health`;
- `task infra:health`;
- `task gitops:preflight`;
- `task ops:post-argocd-check` — формально PASS с ограничением A-21;
- environment, versions, runtime-secret и platform-layout contracts;
- Kustomize bootstrap/platform и observability Helm render;
- documentation, Python tests и Ruff;
- `tofu -chdir=infrastructure fmt -check` и `validate`;
- `git diff --check`.

С ограничениями:

- `task check:validate` останавливается на `check:tfvars-example`: в root
  `terraform.tfvars.example` отсутствуют 10 infrastructure inputs;
- yamllint завершился с десятью существующими line-length warnings.

### Актуальный статус исходных замечаний

- закрыты: **A-01, A-02, A-04, A-05, A-07, A-08, A-10**;
- переоткрыты: **A-03, A-16**;
- частично закрыты или приняты как риск: **A-06, A-09, A-11—A-15,
  A-17, A-18**;
- новые: **A-19, A-20, A-21**.

## Исправление A-03, A-16, A-18 и A-21 — 2026-08-23

Изменения реализованы локально с сохранением ownership boundaries: runtime
network policies находятся в `argocd/`, post-deploy gate — в `homelabctl`,
runtime secrets и bootstrap entrypoints не затронуты.

- **A-03/A-20:** observability egress дополнен отсутствующими service metrics
  ports. Для kubelet и node-local Cilium/Hubble endpoints добавлено явное
  разрешение Cilium entities `host`/`remote-node` на `10250` и `9962—9965`;
  обычный CIDR egress не считается достаточным разрешением node entities.
- **A-16/A-21:** `ops:post-argocd-check` теперь отклоняет незавершённые или
  ошибочные Argo CD operations/hooks, CrashLoopBackOff и неготовые runtime
  Pods, Backup старше трёх часов и scrape/export errors OpenTelemetry за
  последние две минуты. Порог Backup настраивается переменной
  `POST_CHECK_MAX_BACKUP_AGE_HOURS`. Добавлены unit-тесты на Stalwart
  CrashLoop/incomplete operation, свежесть Backup и telemetry errors.
- **A-18:** длинные shell-команды Forgejo SSO Job разбиты без изменения
  поведения. Для атомарных embedded Grafana JSON/PromQL выражений добавлено
  локальное документированное исключение yamllint; общий lint проходит без
  warnings.

Локально успешно выполнены Python tests/Ruff, yamllint, Kustomize bootstrap и
platform builds, observability Helm render, environment contract и
documentation checks. Полный `task check:validate` по-прежнему останавливается
на ранее зафиксированном ограничении `check:tfvars-example`: в root example
отсутствуют 10 infrastructure inputs; это не связано с данными изменениями.

Live-публикация и повторная проверка завершены. Основное исправление было
опубликовано как GitOps revision `26095560b78b044c7e15c147c727b099ed36b1fd`.
Первый live window подтвердил устранение network timeout, но обнаружил два
следующих дефекта: Talos kubelet serving certificates не содержат IP SAN, а
Grafana scrape/health targets обращались к несуществующему Service-порту
`3000` вместо `80`. Kubelet receiver сохранил service-account authentication
и получил документированное исключение hostname verification; Grafana targets
исправлены на Service-порт `80`. Corrective GitOps revision:
`9c4e6f4cf434d68f5a812d6481d9b59d019707aa`.

На corrective revision `root`, `observability-prereqs`, `otel-collector` и
`otel-agent` достигли `Synced/Healthy`; новые OTel Pods готовы без рестартов.
Повторный `ops:post-argocd-check` подтвердил:

- отсутствие OpenTelemetry scrape/export errors в чистом двухминутном окне;
- свежий completed Velero Backup и `BackupStorageLocation/default=Available`;
- готовность runtime Pods, Garage layout, Argo CD OIDC/RBAC и отсутствие
  Kyverno PolicyReport failures;
- точное совпадение root GitOps revision с `9c4e6f4...`.

Итоговый gate завершился FAIL только по отдельному открытому **A-19**:
Application `stalwart` сохраняет последнюю operation в фазе `Failed`. Это
подтверждает, что **A-16/A-21 закрыты**: проверка больше не выдаёт
ложноположительный PASS. **A-03/A-20 и A-18 также закрыты**.

## Завершение этапа 0 рефакторинга — 2026-08-23

Этап 0 завершён с уточнённым контрактом inputs: переменная OpenTofu является
обязательной только при отсутствии `default`. Все inputs entrypoint
`infrastructure/` имеют defaults и являются опциональными, поэтому они не
должны искусственно дублироваться в root `terraform.tfvars.example`.
`check:tfvars-example` теперь требует документировать только обязательные
inputs; поведение и текущие наборы обязательных переменных закреплены
unit-тестом.

Одновременно закрыто последнее live-замечание **A-19**. Для Stalwart v0.16.16
поле OIDC `requireScopes` является map string-to-bool, а не массивом. Plan и
его генератор исправлены с `["openid", "email"]` на
`{"openid": true, "email": true}`; документация приведена к тому же
контракту.

Проверки завершены успешно:

- полный `task check:validate`, включая оба `tofu validate`, YAML, Kustomize,
  Helm render, Python tests и Ruff;
- публикация GitOps revision
  `b0c285c163e8dac845066b61f5399f495f54463a`;
- `Application/stalwart` — `Synced/Healthy`, последняя operation —
  `Succeeded`;
- PostSync Job `stalwart-authentik-oidc-configuration` — `Succeeded=1`;
- полный `ops:post-argocd-check` — PASS, включая Argo CD operations, runtime
  Pods, Backup/BSL, Garage, OIDC/RBAC, Kyverno, OpenTelemetry и точное
  совпадение GitOps revision.

После этого **A-19 закрыто**, а локальный и live gates этапа 0 зелёные.

## Завершение этапа 1 рефакторинга — 2026-08-24

Этап 1 завершён без изменения secret model: Garage credentials остаются в
OpenBao, ESO материализует `velero-credentials`, а Velero использует
внутрикластерный endpoint
`http://garage-s3.garage.svc.cluster.local:3900`. Live Garage node healthy,
single-node layout `homelab` назначен, а `BackupStorageLocation/default`
имеет фазу `Available`.

Выполнен отдельный smoke cycle:

- `Backup/stage1-echo-20260824` — `Completed`, 32/32 objects;
- `Restore/stage1-echo-20260824` — `Completed`, 3/3 objects, zero errors;
- данные восстановленного `ConfigMap/homelab-root-ca` в namespace
  `echo-restore-stage1-20260824` совпали с источником по SHA-256;
- единственный restore warning относится к автоматически созданному
  Kubernetes `ConfigMap/kube-root-ca.crt` и не влияет на результат.

Smoke restore ограничен ConfigMap objects: это проверяет путь
Garage -> Velero -> Kubernetes API, не создавая дублирующие Gateway,
HTTPRoute или workloads. Test namespace оставлен для операторской проверки;
его удаление остаётся отдельной destructive операцией.

В Grafana добавлены provisioned alerts:

- `Velero Backup Storage Unavailable` по
  `velero_backup_location_status_gauge`;
- `Velero Backup Stale` по
  `velero_backup_last_successful_timestamp` с порогом три часа.

Обе rules используют `noDataState: Alerting`, загружены live Grafana API и на
здоровом состоянии возвращают значение `0`. Во время rollout также устранён
RWO Multi-Attach deadlock: single-replica Grafana Deployment переведён с
`RollingUpdate` на `Recreate`. Для существующего Deployment потребовалась
одноразовая атомарная live-миграция strategy; greenfield manifest сразу
создаётся с `Recreate`.

Runtime changes опубликованы как GitOps revision
`3114831a71e733da37cc582e9406d467cc70d4d4`. Полный `task check:validate` и
финальный `ops:post-argocd-check` прошли успешно. Критерии этапа 1 — BSL
`Available`, новый Backup `Completed`, успешный документированный restore и
alerts на BSL/freshness — выполнены.

## Завершение этапа 2 рефакторинга — 2026-08-24

Этап 2 завершён на live GitOps snapshot
`2a555c1a8fefc364e2891871aade8d357d23f680`. Все семь observability
Applications (`observability-prereqs`, `victoria-metrics`, `loki`, `tempo`,
`otel-collector`, `otel-agent`, `grafana`) имеют состояние `Synced/Healthy`,
Pods готовы, а четыре backend PVC — `Bound`.

Полный `gitops:preflight` выполнен через временный localhost-only OpenBao
port-forward: 20/20 runtime paths healthy, missing paths/keys, shape errors и
provisional markers отсутствуют. Port-forward после проверки остановлен.
Grafana admin и OIDC остаются в OpenBao/ESO; secret values не выводились.
Отдельного production alert webhook в текущем contract нет, поскольку реальный
receiver не выбран. Вместо фиктивного постоянного contact point delivery path
проверен transient Grafana receiver-test API на внутренний `echo` endpoint.

Добавлена воспроизводимая команда `task ops:observability-smoke`, которая:

- отправляет synthetic OTLP/HTTP JSON span в central Collector;
- запрашивает trace через Grafana Tempo datasource и подтверждает round-trip;
- требует роста `otelcol_exporter_sent_spans` без роста
  `otelcol_exporter_send_failed_spans` и с пустой sending queue;
- проверяет наличие provisioned dashboards и ключевых alert rules;
- отправляет одноразовую webhook test notification во внутренний `echo`, не
  создавая contact point/notification policy и не принимая credential-bearing
  URL через process arguments.

Live smoke-test подтвердил trace
`19070a9aaaa9b0c228e2bdf52e85df53` через Grafana -> Tempo, рост successful
export counter, zero exporter failures/queued spans, наличие dashboards/rules
и успешную webhook delivery. Отдельный ручной Grafana API test также получил
`status=success`, а `echo` зарегистрировал POST с alert
`Stage2WebhookSmoke`.

Локальный `task check:validate` прошёл полностью, включая render всех шести
observability charts, Kustomize, YAML, OpenTofu validation и 11 Python tests.
Финальный `ops:post-argocd-check` также завершился PASS: недавних OTel
scrape/export errors нет. Критерии этапа 2 — доступный trace, стабильные
exporter counters и подтверждённая alert delivery — выполнены.

## Завершение этапа 3 рефакторинга — 2026-08-25

Declarative identity chain восстановлен без изменения ownership boundaries:
Argo CD OIDC/RBAC остаётся в `infrastructure/`, Authentik blueprints и
ExternalSecrets — в `argocd/`, а break-glass credential — только в OpenBao.
`tofu -chdir=infrastructure plan -detailed-exitcode` вернул `No changes`,
поэтому infrastructure apply и повторный Helm rollout не требовались.

Live-проверка подтвердила:

- полный `gitops:preflight`: 20/20 OpenBao paths healthy, отсутствующих keys,
  shape errors и provisional paths нет;
- ExternalSecrets `argocd-oidc`, `platform-identity-blueprint`,
  `argocd-sso-blueprint`, `harbor-sso-blueprint` и `grafana-sso-blueprint`
  имеют `Ready=True/SecretSynced`;
- Authentik blueprint instances `platform-identity` и `argocd-sso` имеют
  статус `successful`; Application `argocd`, confidential provider `Argo CD`,
  strict callback и binding группы `platform-admins` согласованы;
- live `oidc.config` использует Secret-backed client fields, ожидаемый issuer
  и scopes; `platform-admins` получает `role:admin`, а default
  `role:authenticated` ограничен read-only доступом к Applications;
- OIDC discovery и redirect Argo CD на Authentik Authorization endpoint
  работают с доверенным homelab CA;
- фактический primary login постоянного administrator с паролем из OpenBao
  принят Authentik и доходит до MFA challenge;
- offline evaluation против live `argocd-rbac-cm` разрешает admin-group
  delete, запрещает его обычному subject, оставляет ему Application read и
  запрещает чтение cluster credentials.

Добавлены две идемпотентные operator-команды:

- `task ops:argocd-access-finalize` проверяет bootstrap login, сохраняет новый
  пароль в `secret/platform/argocd/admin`, ротирует его через Argo CD API,
  повторно проверяет login и только затем удаляет bootstrap Secret;
- `task ops:identity-smoke` повторяет live OIDC/ExternalSecret/blueprint/RBAC,
  primary-auth и break-glass проверки без вывода passwords, tokens или Secret
  data.

На live-кластере break-glass password успешно ротирован и проверен,
`argocd-initial-admin-secret` удалён. Полный Authorization Code callback после
ввода MFA нельзя завершить неинтерактивно без обхода второго фактора; он
остаётся последним ручным подтверждением в private/incognito browser. Все
остальные критерии этапа 3, включая negative RBAC и break-glass lifecycle,
выполнены автоматически и воспроизводимо.

## Завершение этапа 4 рефакторинга — 2026-08-26

Harbor convergence восстановлен без StatefulSet recreation и без изменения
PVC. Rendered и live `StatefulSet/harbor-trivy` совпали после Kubernetes
normalization; server-side dry-run/diff не показал изменений. Все пять Harbor
PVC имеют `Bound`, а перед работами подтверждены свежие completed Velero
backups.

Причина оставшегося Secret diff локализована в upstream chart Harbor 1.19.2:
chart всегда создаёт `Secret/harbor-trivy`, не имеет Trivy `existingSecret`
switch и строит credentialed Redis URL через Helm `lookup`. Argo CD
repo-server рендерит без Kubernetes API access, поэтому ESO должен заменять
ровно `data.redisURL`. Исключение Argo CD сужено с `/data` до
`/data/redisURL`; chart-owned `gitHubToken` снова находится под drift
detection. Маскирования StatefulSet, list order или всего Secret data нет.

Добавлена воспроизводимая команда `task ops:harbor-smoke`, которая без вывода
credentials:

- требует healthy status core, database, jobservice, portal, Redis, registry,
  registryctl и Trivy;
- подтверждает Harbor OIDC redirect и primary Authentik login до MFA;
- копирует Linux/amd64 `pause:3.10` во временный repository внутреннего
  registry и выполняет обратный pull/inspect;
- запускает Trivy scan и требует успешный scan report;
- удаляет временный repository в `finally`;
- требует `Application/harbor=Synced/Healthy` и последнюю operation
  `Succeeded`.

Live smoke успешно подтвердил registry push/pull, Trivy scan с нулём найденных
уязвимостей, OIDC primary authentication до MFA и cleanup test repository.
Controlled recreation не потребовалась: registry и Trivy data на PVC не
затрагивались. Полный browser callback после ввода MFA остаётся ручной
проверкой, поскольку автоматизация не должна обходить второй фактор.

## Завершение этапа 5 рефакторинга — 2026-08-26

Security/image baseline закрыт для управляемого runtime слоя:

- `bitnami/kubectl` переведён с `latest@digest` на digest-only reference;
  metadata закреплённого image сообщает `APP_VERSION=1.36.4`, отдельный
  upstream version tag отсутствует;
- PostgreSQL/Redis/Valkey workload и exporter images закреплены digest через
  chart values; общий render gate запрещает незакреплённые Bitnami images;
- Echo и Garage имеют полный pod/container restricted context и resources;
- Harbor component containers имеют requests/limits и restricted context,
  pod seccomp добавляется узкой Harbor-only mutation policy и подтверждается
  по live Pods;
- Authentik, Forgejo, Grafana, Loki, OTel, VictoriaMetrics и Velero получили
  недостающие pod seccomp, init/sidecar resources и security context;
- Forgejo mutable test Pod отключён, Velero CRD upgrade Job получил resources;
- policies теперь охватывают Authentik, Forgejo, Observability и Velero, а
  controller filter синхронизирован с system-only namespace exclusions;
- единственное workload-level исключение — `velero` Pod с label
  `name=node-agent`: root/hostPath необходимы для filesystem backup; image и
  resources остаются под policy control;
- resource и basic-security policies переведены из Audit-only в
  `Deny + Audit`; ephemeral containers включены в проверки.

Добавлен `task check:runtime-workloads`, который параллельно рендерит 19 Helm
releases и четыре Kustomize/raw workload-набора. Gate проверяет обычные, init,
sidecar и ephemeral containers на image tag/digest, отсутствие `latest`,
digest pin Bitnami images, CPU/memory requests/limits и restricted security
context. Локальный полный render завершён без violations; Kyverno policies
прошли server-side dry-run.

GitOps revision `de003a7d8c88242b0ab568412150a68984621374` применена live.
Итоговая проверка подтвердила `Synced/Healthy` для всех Applications, ноль
live image/resource/security violations и ноль fail/error/warn в
PolicyReports. Отрицательный server-side dry-run в namespace `authentik`
отклонён одновременно latest, resource и basic-security policies.
Observability smoke подтвердил OTLP trace path, exporter counters, Grafana
dashboards/rules и webhook delivery.

Во время rollout single-replica Kyverno webhook кратко потерял endpoint на
фоне нестабильного Kubernetes API/controller-manager leadership. После
восстановления endpoint Argo CD comparison повторён. Kube-controller-manager
не завершил scale-down двух старых Authentik ReplicaSets; новые revisions уже
были Ready, поэтому старые stateless revisions были точечно масштабированы до
нуля. StatefulSets и PVC не затрагивались. Этот эпизод не меняет результат
этапа 5, но подтверждает ранее принятое ограничение single-replica/HA baseline.

## Резюме

Статическая структура GitOps слоя консистентна: 33 platform Applications
индексируются ровно один раз, environment/version/secret contracts сходятся,
Kustomize и observability Helm render проходят. Все live PVC имеют состояние
`Bound`, существующие ExternalSecrets — `SecretSynced`, Certificates —
`Ready`, Gateway и HTTPRoute — `Programmed`/`Accepted`.

При этом live-состояние нельзя считать готовым:

- все 422 объекта Velero Backup имеют фазу `FailedValidation`;
- BackupStorageLocation `default` недоступен;
- Garage layout не инициализирован;
- Tempo не развёрнут, а OTel Collector теряет трейсы;
- Harbor остаётся `OutOfSync` после неуспешной синхронизации;
- live Argo CD не получил ожидаемые OIDC и RBAC настройки;
- локальные исправления observability ещё не опубликованы;
- Kyverno фиксирует нарушения security baseline, а policy и NetworkPolicy
  coverage остаются неполными.

## Замечания

### P0 — восстановление данных и telemetry

#### A-01. Резервное копирование полностью неработоспособно

Наблюдения:

- `BackupStorageLocation/default` имеет фазу `Unavailable`;
- endpoint `https://minio.lab.zerodi.ru` возвращает TLS certificate для
  случайного Traefik/default hostname, а не для `minio.lab.zerodi.ru`;
- оба Velero Schedule включены, но успешных Backup нет;
- все 422 Backup имеют фазу `FailedValidation`;
- PodVolumeBackup отсутствуют.

Следствие: кластер не имеет подтверждённого GitOps-managed backup/restore
контура. Наличие Schedule не является доказательством защиты данных.

#### A-02. Garage не готов как S3 backend

`garage status` показывает single-node instance с состоянием
`NO ROLE ASSIGNED`. Layout, bucket и production S3 key не созданы. Это
предусмотренный ручной этап, но он не был завершён.

#### A-03. Трейсы теряются

Live Application `tempo` имеет `Unknown/Healthy` и `ComparisonError`: старый
`tempo-distributed 3.0.6` требует Kafka для block builder/live store. Tempo
Pods, Service и PVC отсутствуют.

OTel Collector продолжает экспорт в отсутствующий Tempo, сообщает
`sending queue is full`, `Dropping data`, `no more retries left` и DNS errors
для старых `tempo-distributor`/`tempo-query-frontend` endpoints.

Локально подготовлен переход на single-binary chart `tempo 2.2.4`, но он ещё
не опубликован и не проверен live.

### P1 — GitOps convergence и доступ

#### A-04. Live GitOps snapshot отстаёт от рабочего дерева

Root и большинство дочерних Applications синхронизируют snapshot `717edfd`.
Локальные изменения включают новую Tempo topology, `otel-agent`, Grafana
alerting secret, observability NetworkPolicy, metrics collection и security
настройки. Эти ресурсы нельзя считать внедрёнными.

Публикация также заблокирована рабочим процессом репозитория: `task gitops:push`
намеренно откажется работать при незакоммиченном `argocd/` subtree.

#### A-05. Harbor не достигает желаемого состояния

Application `harbor` имеет `OutOfSync/Healthy`. Последняя sync operation
завершилась ошибкой strategic merge patch list order для
`StatefulSet/harbor-trivy`; `Secret/harbor-core` также отображается
`OutOfSync`.

`Healthy` здесь означает, что старые Pods продолжают работать, а не то, что
live соответствует Git.

#### A-06. OIDC и RBAC Argo CD не внедрены live

Live `argocd-cm` не содержит `oidc.config`; live `argocd-rbac-cm` содержит
пустые `policy.csv` и `policy.default`. В то же время repository
`infrastructure/argocd.tf` задаёт Authentik OIDC, group mapping и default
role. Следовательно, desired infrastructure config и live release расходятся.

`argocd-initial-admin-secret` всё ещё существует. До подтверждения SSO и
break-glass доступа удалять или отключать admin нельзя.

#### A-07. Не материализованы обязательные runtime resources

Live отсутствуют:

- `authentik/Secret/platform-identity-blueprint`;
- `argocd/Secret/argocd-oidc`;
- `harbor/Secret/harbor-oidc-config`;
- `observability/Secret/grafana-oidc`;
- `observability/Secret/grafana-alerting`;
- Application `otel-agent`.

Существующие ExternalSecrets исправны. Это GitOps revision gap, а не отказ
ESO/OpenBao. Полный OpenBao preflight во время аудита не выполнялся, потому
что локальный port-forward на `127.0.0.1:8200` не был запущен.

### P2 — security и isolation

#### A-08. Kyverno подтверждает нарушения workload baseline

Live PolicyReports фиксируют:

- Harbor: отсутствующие security context и requests/limits;
- Harbor PostgreSQL/Valkey: mutable `latest` images и security context gaps;
- Echo и Garage: неполный container security context.

У Garage security context отсутствует в repository StatefulSet. У Echo задан
pod-level seccomp, но не заданы обязательные container-level
`runAsNonRoot`, `allowPrivilegeEscalation: false` и `capabilities.drop: ALL`.

#### A-09. Policy coverage неполный

Policies для security context и requests/limits работают только в режиме
`Audit`. Из них исключены многие platform namespaces, включая `argocd`,
`authentik`, `forgejo`, `observability` и `velero`. Поэтому успешный Argo CD
sync не означает прохождение security baseline.

#### A-10. Mutable и transitive images не контролируются полностью

Repository явно использует `bitnami/kubectl:latest`. Live Bitnami
PostgreSQL/Redis/Valkey workloads также используют `latest`.

Текущий `check:versions` подтверждает согласованность объявленных pins, но не
запрещает `latest` и не анализирует все images, сгенерированные Helm charts.

#### A-11. NetworkPolicy coverage недостаточен

Live NetworkPolicy отсутствует в `observability` и `woodpecker`. Полноценного
namespace default-deny baseline нет для Authentik, Forgejo, Harbor, Garage и
Woodpecker.

Подготовленная локальная observability policy всё ещё широка: она разрешает
Grafana/OTLP ingress из любого namespace и весь межnamespace egress. DNS rule
при таком egress становится практически избыточным.

#### A-12. Argo CD использует plaintext для части внутрикластерных соединений

Infrastructure values включают `server.insecure`, plaintext repo-server
connections и отключённый repo-server TLS. NetworkPolicy ограничивает доступ
к repo-server, но `argocd-server` разрешает ingress от любых Pods и не имеет
egress restriction.

#### A-13. AppProject `platform` имеет большой blast radius

Project разрешает любые namespaced resources, а также Namespace, CRD,
ClusterRole, ClusterRoleBinding, ClusterIssuer, GatewayClass и cluster-wide
policies. Один project объединяет все platform domains и множество внешних
Helm repositories.

#### A-14. Git revision и автоматизация увеличивают риск публикации

Root и дочерние Applications используют mutable `targetRevision: main` вместе
с автоматическими `prune` и `selfHeal`. Ошибка в опубликованном subtree может
автоматически распространиться на весь runtime слой.

### P3 — reliability и verification

#### A-15. Argo CD не имеет HA baseline

Live развёрнуты по одному application-controller, server, repo-server и Redis.
Для homelab это допустимый компромисс, но потеря соответствующего worker или
плановое обновление временно остановит reconciliation/UI.

#### A-16. Post-deploy audit даёт неполную гарантию

`task ops:post-argocd-check` пока не проверяет:

- фазу BackupStorageLocation и возраст последнего успешного Backup;
- Garage layout/bucket readiness;
- наличие и readiness Tempo workload/PVC;
- ошибки или drop counters OTel exporters;
- Kyverno PolicyReport failures;
- live OIDC/RBAC configuration Argo CD;
- соответствие ожидаемому GitOps snapshot.

Поэтому большая часть Applications отображается `Healthy`, когда backup и
tracing фактически не работают.

#### A-17. Validation покрывает Helm charts неравномерно

Есть отдельный Helm render check для observability, но аналогичного полного
render/policy validation нет для Harbor, Forgejo, Authentik, Woodpecker,
Velero и их Bitnami dependencies. Это позволяет пропустить неэффективные
values keys, transitive `latest` images и missing security/resources.

#### A-18. Неблокирующие YAML warnings

Yamllint прошёл с десятью предупреждениями о строках длиннее 160 символов в
Grafana values и Forgejo SSO Job. Функциональной ошибки нет, но такие строки
ухудшают review и сопровождение embedded JSON/scripts.

## План исправления

План рассчитан на greenfield ownership model репозитория. Runtime manifests
остаются в `argocd/`, Argo CD Helm release и его OIDC/RBAC config — в
`infrastructure/`, secrets — только в OpenBao с доставкой через ESO.

### Этап 0. Зафиксировать безопасную точку изменения

Цель: исключить случайную публикацию смешанного рабочего дерева.

1. Разделить текущие изменения на логические наборы: observability,
   application metrics/security, audit tooling и документация.
2. Проверить diff каждого набора на secret values и несвязанные изменения.
3. Закоммитить `argocd/` только после прохождения локальных checks.
4. Зафиксировать ожидаемый GitOps commit/snapshot для последующей live-проверки.

Проверки:

```bash
task check:env-contract
task check:versions
task check:runtime-secret-contract
task check:kustomize-bootstrap
task check:kustomize-platform
task check:helm-observability
task check:yamllint
```

Критерий готовности: `argocd/` clean, публикация содержит только проверенные
runtime changes, secret values отсутствуют.

### Этап 1. Восстановить Garage и Velero

Цель: получить хотя бы один проверенный backup до остальных рискованных
rollout/recreate операций.

1. Определить канонический Velero S3 endpoint. Для внутрикластерного consumer
   использовать `http://garage-s3.garage.svc.cluster.local:3900`, если внешний
   TLS endpoint не нужен по архитектурному контракту.
2. Если внешний endpoint сохраняется, исправить DNS/Gateway routing и
   подтвердить certificate chain для `minio.lab.zerodi.ru`.
3. Получить Garage node ID и вручную назначить single-node layout согласно
   `docs/garage-velero.md`.
4. Создать bucket `homelab-velero` и S3 key `velero`, выдать минимальные права.
5. Записать production credentials в `secret/platform/velero/s3`, не выводя
   их в Git, logs или shell history; удалить provisional marker штатной
   перезаписью OpenBao path.
6. Дождаться ESO refresh и состояния BSL `Available`.
7. Создать smoke backup namespace `echo`, дождаться `Completed`.
8. Выполнить restore smoke-test в отдельный временный namespace и проверить
   восстановленные объекты.
9. Добавить alert на недоступный BSL и отсутствие свежего успешного backup.

Критерий готовности: BSL `Available`, новый Backup `Completed`, restore test
успешен и задокументирован.

Ручные границы: layout, bucket/key, OpenBao writes и restore требуют явного
операторского подтверждения. Удаление test namespace — отдельная destructive
операция.

### Этап 2. Опубликовать и восстановить observability

Цель: прекратить потерю telemetry и внедрить подготовленные изменения.

1. Перед публикацией проверить OpenBao contract для Grafana admin, OIDC и
   alert webhook; не выводить значения.
2. Повторно отрендерить Tempo, OTel Collector, OTel Agent, Loki и Grafana.
3. Проверить generated manifests на PVC, probes, service account token,
   security context, resources и ожидаемые service ports.
4. Опубликовать отдельный observability commit через штатный `gitops:push`.
5. Дождаться `Synced/Healthy` для `observability-prereqs`, `tempo`,
   `otel-agent`, `otel-collector`, `loki`, `victoria-metrics`, `grafana`.
6. Проверить Tempo PVC, OTLP ingest, trace query из Grafana и отсутствие
   exporter queue drops.
7. Проверить Grafana dashboards, provisioned alert rules и тестовую доставку
   webhook notification.

Критерий готовности: тестовый trace доступен в Grafana/Tempo, OTel drop/error
counters не растут, alert delivery подтверждена.

### Этап 3. Восстановить identity chain и Argo CD access

Цель: привести live Argo CD к declarative OIDC/RBAC contract без потери
break-glass доступа.

1. Запустить OpenBao port-forward и выполнить полный `task gitops:preflight`.
2. Убедиться, что paths `platform/argocd/oidc`, Authentik platform identity,
   Harbor OIDC и Grafana OIDC содержат ожидаемые keys.
3. Опубликовать/sync соответствующие ExternalSecrets и Authentik blueprints.
4. Дождаться `Secret/argocd-oidc` и успешного применения Authentik provider.
5. Выполнить `tofu -chdir=infrastructure plan` и проверить только ожидаемые
   изменения Argo CD Helm release.
6. После отдельного разрешения применить infrastructure plan.
7. Проверить `oidc.config`, RBAC mapping группы `platform-admins` и успешный
   SSO login.
8. Проверить local admin/break-glass доступ, ротировать пароль и только после
   этого удалить `argocd-initial-admin-secret`.

Критерий готовности: SSO работает, пользователь вне admin group не получает
admin, `platform-admins` получает ожидаемую роль, break-glass проверен,
bootstrap secret отсутствует.

### Этап 4. Исправить Harbor convergence

Цель: получить `Synced/Healthy` без потери registry/trivy данных.

1. Отрендерить Harbor chart с текущими values и сравнить desired/live
   `harbor-trivy` StatefulSet и `harbor-core` Secret metadata без чтения
   secret data.
2. Найти дублирующиеся или меняющие порядок env/volumeMount list entries и
   устранить их на уровне values.
3. Не маскировать ошибку постоянным `ignoreDifferences`, пока причина patch
   order не устранена.
4. Сначала выполнить server-side dry-run/diff.
5. Если требуется controlled recreation StatefulSet, подтвердить сохранность
   PVC и наличие успешного backup из этапа 1; запросить отдельное разрешение.
6. Sync Harbor и проверить core, registry, jobservice, portal, Trivy, push/pull
   smoke-test и OIDC login.

Критерий готовности: Harbor `Synced/Healthy`, sync history успешна, registry
push/pull и Trivy работают.

### Этап 5. Закрыть security и image gaps

Цель: получить нулевое число новых baseline violations в управляемых
workloads.

1. Заменить `bitnami/kubectl:latest` на immutable version или digest.
2. Явно pin все Bitnami workload/exporter images, которые charts сейчас
   рендерят как `latest`.
3. Добавить полноценный container security context в Echo и Garage.
4. Для Harbor задать component-specific security/resources values и
   подтвердить их по rendered Pods, а не только по values file.
5. Добавить resources/security context для init containers и sidecars.
6. Расширить policy audit на platform namespaces с документированными точечными
   исключениями.
7. После нулевого/принятого набора violations переводить security/resource
   policies из `Audit` в `Deny` поэтапно.

Критерий готовности: rendered и live workloads не используют `latest`, новые
PolicyReports не содержат необъяснённых failures, rollout не блокируется
Kyverno.

### Этап 6. Ввести namespace network isolation

Цель: перейти от выборочных policies к default-deny и явным service flows.

1. Составить матрицу ingress/egress для каждого namespace.
2. Добавить default-deny ingress/egress в Authentik, Forgejo, Harbor, Garage,
   Woodpecker и Observability.
3. Разрешать Gateway traffic по namespace/pod labels, а не из любого
   namespace.
4. Ограничить observability ingress конкретными producers и Gateway; убрать
   unrestricted inter-namespace egress.
5. Явно разрешить DNS, Kubernetes API, OpenBao/ESO, database/cache,
   OTLP/metrics/logs и необходимые внешние HTTPS/SMTP endpoints.
6. Проверять policies по одному namespace с connectivity smoke-tests.

Критерий готовности: default-deny действует, все задокументированные потоки
работают, неожиданный cross-namespace connection блокируется.

#### Выполнено 2026-08-26

Этап 6 реализован и проверен live на GitOps snapshot
`b57850f51f2b716893ea7e1a4bd41159e4037fdd`.

- namespace-wide ingress/egress `default-deny` действует в `authentik`,
  `forgejo`, `harbor`, `garage`, `woodpecker` и `observability`;
- chart-owned permissive PostgreSQL/Redis/Valkey policies отключены и удалены
  live; database/cache ingress разрешён только workload selectors и
  OTel Collector на metrics ports;
- Cilium Gateway ingress ограничен entity `ingress`, backend selector и
  container port; Kubernetes API, kubelet, node metrics и external egress
  ограничены Cilium entities и точными ports;
- observability ingress принимает OTLP только от Harbor, OTel Agent и
  synthetic Grafana producer; scrape/export/data-source flows перечислены
  явно, unrestricted inter-namespace egress удалён;
- OpenBao/ESO не добавлены как application peers: ESO материализует Secrets
  через Kubernetes API, workload Pods напрямую к OpenBao не подключаются;
- полная flow-матрица и rollout procedure сохранены в
  `docs/network-isolation.md`.

Live-проверка:

- `task ops:post-argocd-check` — полностью успешно, включая ready workloads,
  свежий `Completed` Velero Backup, Garage layout, Kyverno PolicyReports и
  отсутствие свежих OTel scrape/export errors;
- `task ops:observability-smoke` — synthetic OTLP trace найден через
  Grafana/Tempo, exporter counters без failures/queue, dashboards/rules готовы,
  webhook доставлен в Echo;
- Gateway health: Authentik `200`, Forgejo `200`, Harbor `200`, Woodpecker
  `204`, Grafana `200`; Garage S3 вернул ожидаемый unauthenticated `403`;
- отрицательный smoke из Forgejo в
  `garage-s3.garage.svc.cluster.local:3900` завершился connection timeout,
  подтверждая блокировку неожиданного cross-namespace соединения;
- server-side dry-run всех шести prereqs subtrees и `task check:validate`
  прошли; Python suite — 16 tests.

Во время первого live gate выявлены и точечно добавлены два отсутствующих
операционных flow: OTel Collector к Cilium `remote-node` на `9962-9965` и
Grafana к Collector на `4318/8888` для synthetic smoke. Общий egress при этом
не расширялся.

### Этап 7. Уменьшить blast radius Argo CD

Цель: ограничить последствия компрометации Git/Helm source или ошибочного
commit.

1. Разделить `platform` AppProject минимум по cluster-scoped bootstrap и
   namespaced runtime domains.
2. Сузить destinations и namespace resource whitelist каждого project.
3. Оставить CRD/ClusterRole/ClusterRoleBinding только Application, которым они
   действительно нужны.
4. Рассмотреть immutable Git revision для release promotion вместо прямого
   auto-sync `main`.
5. Добавить sync windows или staged promotion для storage, identity и policy
   changes.
6. Оценить включение TLS для внутренних Argo CD connections; если plaintext
   сохраняется, сузить server/repo-server NetworkPolicies.
7. Зафиксировать решение по single-replica/HA baseline для homelab.

Критерий готовности: каждый Application имеет минимально необходимые source,
destination и resource permissions; критические изменения не попадают live
одним непроверенным push.

#### Реализация этапа 7 — 2026-08-27

- единый `platform` удалён и заменён domain/app-specific projects; вместе с
  суженными `bootstrap`, `apps` и orchestration projects bootstrap содержит 19
  AppProjects без wildcard resource permissions;
- CRD и cluster RBAC оставлены только gateway, Reloader, Authentik,
  Grafana/Loki/OTel, Kyverno controller/policies и Velero — согласно
  инвентаризации фактически управляемых cluster-scoped kinds;
- все 37 Applications проверены на существующий project, разрешённый
  source и точный destination namespace;
- 11 Applications в identity/storage/policy закреплены на проверенном
  immutable Forgejo snapshot
  `91cd52870c84700b405303ddb80084d7a5b4165f`; optional
  `gitops.critical_revision` синхронизирует этот pointer, а strict preflight
  требует полный 40-символьный SHA;
- critical projects получили постоянно активное deny sync window с
  `manualSync: true`: candidate push не меняет critical workloads, promotion
  требует отдельного pointer commit и ручного sync;
- repo-server TLS переведён с non-validating на strict validation; Certificate
  выпускается `homelab-ca` до Helm release. Пять infrastructure-owned
  NetworkPolicy ограничивают component ingress и server/repo-server egress;
- single-replica homelab baseline зафиксирован явно для controller, server,
  repo-server, ApplicationSet и Redis; Redis HA отключён. Условия перехода на
  HA и promotion/rollback описаны в `docs/argocd-hardening.md`;
- локально прошли infrastructure ownership, layout, version, documentation,
  environment contract, OpenTofu validate/fmt, YAML, Kustomize и Python gates
  (17 tests). Полный baseline остановился только на сетевом скачивании
  VictoriaMetrics chart после истечения HTTP timeout.

Для существующего live-кластера ownership namespace `argocd` требует
однократного `tofu import`; публикация, infrastructure apply и повторный live
audit фиксируются ниже после восстановления маршрута к `192.168.110.200`.

### Этап 8. Расширить автоматические проверки

Цель: сделать live audit доказательством работоспособности, а не только
наличия Kubernetes objects.

Добавить в repository checks:

1. Helm render всех runtime charts и их dependencies.
2. Проверку rendered images на `latest`/missing tag и опционально на digest.
3. Policy validation rendered Pods для resources/security context.
4. Проверку BSL `Available`, свежего `Completed` Backup и результата
   периодического restore test.
5. Проверку Garage layout/bucket readiness без вывода credentials.
6. Проверку Tempo health, PVC и synthetic trace round-trip.
7. Проверку OTel exporter failure/drop counters.
8. Проверку Kyverno PolicyReports с allowlist принятых исключений.
9. Проверку Argo CD OIDC/RBAC config и bootstrap secret lifecycle.
10. Проверку ожидаемого GitOps snapshot/revision.

Критерий готовности: `task ops:post-argocd-check` падает на каждом из
состояний A-01—A-07 и предоставляет короткую причину без secret values.

#### Реализация этапа 8 — 2026-08-27

- `task check:runtime-workloads` остаётся обязательной частью общего baseline:
  gate сверяет полный список chart Applications, параллельно рендерит 19 Helm
  releases с dependencies и Kustomize/raw workloads, затем проверяет все Pod
  templates на tagged/digest images, отсутствие `latest`, digest для Bitnami,
  resources и restricted security context;
- добавлен явный `task ops:backup-restore-smoke`: он создаёт ограниченный
  ConfigMap backup/restore из `echo` в уникальный namespace, сравнивает
  SHA-256 содержимого и только после успешной проверки ставит Restore метку
  `homelab.dev/restore-verified=true`; cleanup остаётся operator checkpoint;
- post-check требует `BSL/default=Available`, свежие Completed Backup и
  verified Restore, назначенный Garage layout и bucket из environment
  contract, ready Tempo и `storage-tempo-0=Bound`;
- synthetic OTLP trace, запрос через Grafana/Tempo, рост sent-spans counter,
  отсутствие exporter failures/queue и internal webhook теперь входят в
  основной post-check, а не остаются отдельным необязательным smoke;
- PolicyReport и ClusterPolicyReport проверяются по результатам
  `fail/error/warn`; точные принятые исключения вынесены в
  `argocd/audit/kyverno-policy-allowlist.yaml`, wildcard запрещён и текущий
  allowlist пуст;
- OIDC/RBAC валидируются по полному declarative contract и environment issuer,
  а `argocd-initial-admin-secret` обязан отсутствовать;
- root Application обязан совпадать с `EXPECTED_GITOPS_REVISION` или локальным
  HEAD, а все 11 critical Applications — одновременно указывать и фактически
  использовать `gitops.critical_revision`;
- ошибки выводятся как короткие имена ресурсов/условий; Secret data и
  credentials не читаются и не печатаются.

Таким образом один `task ops:post-argocd-check` теперь покрывает причины
A-01—A-07. Restore остаётся намеренно отдельной mutating командой, но его
проверенный результат и возраст являются обязательным read-only условием
основного gate.

Первый запуск нового gate против live-кластера подтвердил все новые read-only
и synthetic проверки: BSL/Backup, Garage layout/bucket, Tempo PVC и trace
round-trip, OTel counters/logs, PolicyReports, OIDC/RBAC и bootstrap Secret
lifecycle. Gate ожидаемо завершился FAIL по двум независимым незавершённым
operator checkpoints: существующий успешный Restore этапа 1 ещё не имеет
криптографической verified-метки нового helper, а live root/critical
Applications остаются на snapshot до реализации этапа 7. Новый restore и
публикация/sync не запускались автоматически.

## Общая последовательность выполнения

```text
clean/validate changes
  -> Garage layout and working Velero backup/restore
  -> publish observability and stop trace loss
  -> materialize identity secrets and apply Argo CD OIDC/RBAC
  -> fix Harbor convergence
  -> image/security remediation
  -> namespace NetworkPolicy rollout
  -> AppProject/Argo CD hardening
  -> expand CI and live audit gates
```

Этапы, меняющие live cluster, выполняются только после review diff/plan и
явного разрешения. Storage recreation, restore, secret writes, admin secret
deletion и Garage layout остаются отдельными operator checkpoints.

## Выполненные проверки аудита

Успешно:

- `task check:env-contract`;
- `task check:platform-layout` — 33 Applications indexed exactly once;
- `task check:versions`;
- `task check:runtime-secret-contract`;
- `task check:kustomize-bootstrap`;
- `task check:kustomize-platform`;
- `task check:helm-observability`;
- `task check:documentation`;
- `task check:python` — 5 tests, Ruff format/lint;
- `git diff --check`;
- live Certificate, Gateway, HTTPRoute, PVC, ExternalSecret и Pod inventory.

С ограничениями:

- `task check:yamllint` завершился только с десятью line-length warnings;
- strict environment validation прошла, но полный `task gitops:preflight`
  остановился на недоступном локальном OpenBao port-forward;
- live audit был read-only, поэтому sync/apply/backup/restore не выполнялись.
