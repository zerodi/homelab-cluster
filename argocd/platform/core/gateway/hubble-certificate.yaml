apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-tls
  namespace: gateway
spec:
  secretName: hubble-tls
  dnsNames:
    - hubble.lab.zerodi.ru
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
