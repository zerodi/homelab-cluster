apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: authentik-tls
  namespace: authentik
spec:
  secretName: authentik-tls
  dnsNames:
    - auth.lab.zerodi.ru
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
