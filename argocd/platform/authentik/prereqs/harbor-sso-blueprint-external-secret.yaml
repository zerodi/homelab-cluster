apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-sso-blueprint
  namespace: authentik
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: harbor-sso-blueprint
    creationPolicy: Owner
    template:
      engineVersion: v2
      templateFrom:
        - target: Data
          configMap:
            name: platform-sso-blueprint-templates
            items:
              - key: harbor.yaml
                templateAs: Values
  data:
    - secretKey: client_id
      remoteRef:
        key: platform/harbor/oidc
        property: client_id
    - secretKey: client_secret
      remoteRef:
        key: platform/harbor/oidc
        property: client_secret
