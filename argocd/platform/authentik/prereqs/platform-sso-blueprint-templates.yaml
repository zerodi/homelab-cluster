apiVersion: v1
kind: ConfigMap
metadata:
  name: platform-sso-blueprint-templates
  namespace: authentik
data:
  forgejo.yaml: |
    version: 1
    metadata:
      name: forgejo-sso
      labels:
        blueprints.goauthentik.io/instantiate: "true"
        blueprints.goauthentik.io/description: "Forgejo OIDC provider and application"
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        state: present
        identifiers:
          name: "Authentik"
        attrs:
          name: "Authentik"
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          client_type: confidential
          grant_types:
            - authorization_code
          client_id: "{{ .client_id }}"
          client_secret: "{{ .client_secret }}"
          access_code_validity: "minutes=10"
          access_token_validity: "minutes=30"
          refresh_token_validity: "days=30"
          include_claims_in_id_token: true
          issuer_mode: per_provider
          redirect_uris:
            - matching_mode: strict
              url: "https://git.lab.zerodi.ru/user/oauth2/authentik/callback"
              redirect_uri_type: authorization
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      - model: authentik_core.application
        state: present
        identifiers:
          slug: forgejo
        attrs:
          name: Forgejo
          slug: forgejo
          policy_engine_mode: any
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, "Authentik"]]
          meta_launch_url: "https://git.lab.zerodi.ru/"
          open_in_new_tab: true
  grafana.yaml: |
    version: 1
    metadata:
      name: grafana-sso
      labels:
        blueprints.goauthentik.io/instantiate: "true"
        blueprints.goauthentik.io/description: "Grafana OIDC provider and application"
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        state: present
        identifiers:
          name: "Grafana"
        attrs:
          name: "Grafana"
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token
          client_id: "{{ .client_id }}"
          client_secret: "{{ .client_secret }}"
          access_code_validity: "minutes=10"
          access_token_validity: "minutes=30"
          refresh_token_validity: "days=30"
          include_claims_in_id_token: true
          issuer_mode: per_provider
          redirect_uris:
            - matching_mode: strict
              url: "https://grafana.lab.zerodi.ru/login/generic_oauth"
              redirect_uri_type: authorization
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, offline_access]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, entitlements]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      - model: authentik_core.application
        state: present
        identifiers:
          slug: grafana
        attrs:
          name: Grafana
          slug: grafana
          policy_engine_mode: any
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, "Grafana"]]
          meta_launch_url: "https://grafana.lab.zerodi.ru/"
          open_in_new_tab: true
  argocd.yaml: |
    version: 1
    metadata:
      name: argocd-sso
      labels:
        blueprints.goauthentik.io/instantiate: "true"
        blueprints.goauthentik.io/description: "Argo CD OIDC provider and application"
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        state: present
        identifiers:
          name: "Argo CD"
        attrs:
          name: "Argo CD"
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          client_type: confidential
          grant_types:
            - authorization_code
          client_id: "{{ .client_id }}"
          client_secret: "{{ .client_secret }}"
          access_code_validity: "minutes=10"
          access_token_validity: "minutes=30"
          refresh_token_validity: "days=30"
          include_claims_in_id_token: true
          issuer_mode: per_provider
          redirect_uris:
            - matching_mode: strict
              url: "https://argocd.lab.zerodi.ru/auth/callback"
              redirect_uri_type: authorization
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      - model: authentik_core.application
        state: present
        identifiers:
          slug: argocd
        attrs:
          name: Argo CD
          slug: argocd
          policy_engine_mode: any
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, "Argo CD"]]
          meta_launch_url: "https://argocd.lab.zerodi.ru/"
          open_in_new_tab: true
  harbor.yaml: |
    version: 1
    metadata:
      name: harbor-sso
      labels:
        blueprints.goauthentik.io/instantiate: "true"
        blueprints.goauthentik.io/description: "Harbor OIDC provider and application"
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        state: present
        identifiers:
          name: "Harbor"
        attrs:
          name: "Harbor"
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token
          client_id: "{{ .client_id }}"
          client_secret: "{{ .client_secret }}"
          access_code_validity: "minutes=10"
          access_token_validity: "minutes=30"
          refresh_token_validity: "days=30"
          include_claims_in_id_token: true
          issuer_mode: per_provider
          redirect_uris:
            - matching_mode: strict
              url: "https://harbor.lab.zerodi.ru/c/oidc/callback"
              redirect_uri_type: authorization
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, offline_access]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
      - model: authentik_core.application
        state: present
        identifiers:
          slug: harbor
        attrs:
          name: Harbor
          slug: harbor
          policy_engine_mode: any
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, "Harbor"]]
          meta_launch_url: "https://harbor.lab.zerodi.ru/"
          open_in_new_tab: true
