apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: forgejo-valkey-auth
  namespace: forgejo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: openbao
  target:
    name: forgejo-valkey-auth
    creationPolicy: Owner
  data:
    - secretKey: valkey-password
      remoteRef:
        key: platform/forgejo/valkey
        property: password
