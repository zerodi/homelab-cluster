apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: harbor-tls
  namespace: harbor
spec:
  secretName: harbor-tls
  dnsNames:
    - harbor.home.arpa
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
