apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: garage-tls
  namespace: garage
spec:
  secretName: garage-tls
  dnsNames:
    - garage.lab.zerodi.ru
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
