apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: disallow-hostpath-volumes
  annotations:
    policies.kyverno.io/title: Disallow hostPath volumes
    policies.kyverno.io/category: Baseline
    policies.kyverno.io/severity: high
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      Запрещает hostPath volumes вне явно исключённых namespace'ов.
spec:
  evaluation:
    background:
      enabled: true
  validationActions:
    - Deny
    - Audit
  autogen:
    podControllers:
      controllers:
        - deployments
        - daemonsets
        - statefulsets
        - jobs
        - cronjobs
        - replicasets
        - replicationcontrollers
  matchConstraints:
    namespaceSelector:
      matchExpressions:
        - key: kubernetes.io/metadata.name
          operator: NotIn
          values:
            - argocd
            - cert-manager
            - external-secrets
            - gateway
            - kube-system
            - kyverno
            - openbao
            - piraeus-datastore
            - reloader
            - trust-manager
    resourceRules:
      - apiGroups:
          - ""
        apiVersions:
          - v1
        operations:
          - CREATE
          - UPDATE
        resources:
          - pods
  validations:
    - message: hostPath volumes are not allowed outside excluded namespaces.
      expression: >-
        (object.metadata.namespace == 'velero' &&
        has(object.metadata.labels) &&
        'name' in object.metadata.labels &&
        object.metadata.labels['name'] == 'node-agent') ||
        !has(object.spec.volumes) ||
        object.spec.volumes.all(v, !has(v.hostPath))
