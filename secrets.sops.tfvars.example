proxmox_api_token = "terraform@pve!talos=REDACTED"

# Optional. Needed only if you explicitly create an Argo CD cluster Secret
# through populate instead of relying on the in-cluster default.
# populate_cluster_bearer_token = "REDACTED"
